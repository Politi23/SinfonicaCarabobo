---
interface Props {
    title: string;
    price: string;
    features: string[];
    isPlatinum?: boolean;
    color?: string;
}

const {
    title,
    price,
    features,
    isPlatinum = false,
    color = "bg-gray-100",
} = Astro.props;
---

<div
    class="relative flex flex-col p-8 rounded-3xl transition-all duration-300 bg-white hover:shadow-xl border border-gray-100 h-full"
>
    <div class="mb-8">
        <h3
            class="text-xl font-bold mb-2 uppercase tracking-wide text-gray-500"
        >
            {title}
        </h3>
        <div class="flex items-baseline gap-1">
            <span
                class="text-4xl md:text-5xl font-display font-bold text-primary"
                >{price}</span
            >
            <span class="text-sm text-gray-500">USD</span>
        </div>
    </div>

    <ul class="space-y-4 mb-8 flex-grow">
        {
            features.map((feature) => (
                <li class="flex items-start gap-3">
                    <svg
                        class="w-5 h-5 flex-shrink-0 mt-0.5 text-primary"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M5 13l4 4L19 7"
                        />
                    </svg>
                    <span class="text-sm leading-relaxed text-gray-600">
                        {feature}
                    </span>
                </li>
            ))
        }
    </ul>

    <a
        href="mailto:sinfocarabobo@gmail.com"
        class="w-full py-3 px-6 rounded-xl font-bold text-center transition-all duration-300 transform hover:-translate-y-1 bg-gray-900 text-white hover:bg-primary"
    >
        Elegir Plan
    </a>
</div>
