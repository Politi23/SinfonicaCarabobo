---
import SectionTitle from "@components/ui/SectionTitle.astro";

interface Props {
  subtitle?: string;
  title?: string;
  description?: string;
}

const {
  subtitle = "Eventos Musicales",
  title = "Álbumes de Eventos",
  description = "Explora nuestra colección de álbumes fotográficos de conciertos, presentaciones y eventos musicales especiales.",
} = Astro.props;
---

<div class="max-w-4xl mx-auto mb-12">
  <span
    class="text-secondary text-xs font-bold uppercase tracking-[0.4em] mb-4 block italic"
  >
    {subtitle}
  </span>
  <SectionTitle
    title={title}
    variant="red"
    class="text-3xl md:text-5xl font-display font-bold text-gray-900 mb-4"
    wrapperClass="text-left"
    dividerClass="h-1 w-16"
  />
</div>
