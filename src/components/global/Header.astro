---
const navLinks = [
  { href: "/", label: "Inicio", active: false },
  {
    href: "/nosotros",
    label: "Nosotros",
    active: false,
    dropdown: [
      { href: "/nosotros/reconocimientos", label: "Reconocimientos" },
      { href: "/nosotros/discografia", label: "Discografia" },
    ],
  },
  { href: "/agrupaciones", label: "Agrupaciones", active: false },
  { href: "/programas", label: "Programas", active: false },
  { href: "/blog", label: "Blog", active: false },
  { href: "/libro", label: "Libro", active: false },
  { href: "/apoyanos", label: "Apóyanos", active: false },
  { href: "/contacto", label: "Contacto", active: false },
];
---

<!-- Header con efecto glassmorphism -->
<header class="header" id="main-header">
  <div class="header-backdrop"></div>
  <div class="header-container">
    <!-- Logo -->
    <a href="/" class="logo-link group">
      <div class="flex items-center gap-4">
        <img
          src="/logosimple.webp"
          alt="Logo de la Fundación Orquesta Sinfónica de Carabobo"
          class="logo-image drop-shadow-lg transition-transform duration-300 group-hover:scale-105"
        />
        <div class="hidden md:flex flex-col">
          <span
            class="text-sm font-bold tracking-wide text-white leading-tight font-serif"
          >
            Fundación
          </span>
          <span
            class="text-sm font-bold tracking-wide text-white leading-tight font-serif"
          >
            Orquesta Sinfónica
          </span>
          <span
            class="text-sm font-bold tracking-wide text-white leading-tight font-serif"
          >
            de Carabobo
          </span>
        </div>
      </div>
    </a>

    <!-- Navegación principal -->
    <nav class="main-nav" id="main-nav">
      <!-- Botón hamburguesa para móvil -->
      <button
        class="nav-toggle"
        id="nav-toggle"
        aria-label="Abrir menú de navegación"
        aria-expanded="false"
      >
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>

      <!-- Enlaces de navegación -->
      <ul class="nav-list" id="nav-list">
        {
          navLinks.map((link) => (
            <li class="nav-item">
              {link.dropdown ? (
                <div class="nav-dropdown">
                  <a
                    href={link.href}
                    class={`nav-link ${link.active ? "nav-link-active" : ""}`}
                  >
                    <div class="nav-link-content">
                      <span class="nav-link-text">{link.label}</span>
                      <span class="nav-dropdown-arrow" />
                    </div>
                    <span class="nav-link-underline" />
                  </a>
                  <ul class="nav-dropdown-menu">
                    {link.dropdown.map((subLink) => (
                      <li class="nav-dropdown-item">
                        <a href={subLink.href} class="nav-dropdown-link">
                          {subLink.label}
                        </a>
                      </li>
                    ))}
                  </ul>
                </div>
              ) : (
                <a
                  href={link.href}
                  class={`nav-link ${link.active ? "nav-link-active" : ""}`}
                >
                  <span class="nav-link-text">{link.label}</span>
                  <span class="nav-link-underline" />
                </a>
              )}
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<style>
  /* Variables CSS */
  :root {
    --header-height: 80px;
    --header-height-mobile: 64px;
    --gold: #c28f24; /* primary */
    --gold-light: #ddb850; /* primary-light */
    --gold-dark: #9a7219; /* tono oscuro del dorado */
    --red-accent: #82181a; /* secondary */
    --black: #000000; /* background-dark */
    --black-soft: #0a0a0a; /* surface-dark más claro */
    --white: #ffffff; /* text-light */
    --gray-light: #e0e0e0; /* text-muted */
    --gray: #888888; /* color de texto alternativo */
  }

  /* ============================================
     HEADER STYLES
  ============================================ */
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: var(--header-height);
    z-index: 1000;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .header-backdrop {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.95) 0%,
      rgba(0, 0, 0, 0.85) 50%,
      rgba(0, 0, 0, 0.7) 100%
    );
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    transition: all 0.4s ease;
  }

  .header-container {
    position: relative;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 32px;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* Logo styles */
  .logo-link {
    position: relative;
    display: flex;
    align-items: center;
    text-decoration: none;
    z-index: 10;
  }

  .logo-image {
    height: 56px;
    width: auto;
    object-fit: contain;
    filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.3));
  }

  /* Navigation */
  .main-nav {
    display: flex;
    align-items: center;
  }

  .nav-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 40px;
    height: 40px;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    padding: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 100;
  }

  .nav-toggle:hover {
    border-color: var(--gold);
    background: rgba(194, 143, 36, 0.1);
  }

  .hamburger-line {
    display: block;
    width: 100%;
    height: 2px;
    background: var(--white);
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  .nav-toggle.active .hamburger-line:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .nav-toggle.active .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .nav-toggle.active .hamburger-line:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }

  .nav-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 10px 18px;
    text-decoration: none;
    color: var(--gray-light);
    font-family: "Merriweather", serif;
    font-size: 0.9rem;
    font-weight: 400;
    letter-spacing: 0.02em;
    transition: all 0.3s ease;
    overflow: hidden;
  }

  .nav-link-text {
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
  }

  .nav-link-underline {
    position: absolute;
    bottom: 6px;
    left: 50%;
    transform: translateX(-50%) scaleX(0);
    width: 30px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    transition: transform 0.3s ease;
    border-radius: 2px;
  }

  .nav-link:hover {
    color: var(--white);
  }

  .nav-link:hover .nav-link-underline {
    transform: translateX(-50%) scaleX(1);
  }

  .nav-link-active {
    color: var(--gold);
  }

  .nav-link-active .nav-link-underline {
    transform: translateX(-50%) scaleX(0);
  }

  .nav-link-content {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  /* Dropdown menu styles */
  .nav-dropdown {
    position: relative;
  }
  .nav-dropdown-arrow {
    margin-left: 4px;
    font-size: 0.7rem;
    transition: transform 0.3s ease;
  }
  .nav-dropdown:hover .nav-dropdown-arrow {
    transform: rotate(180deg);
  }
  .nav-dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 10px;
    min-width: 200px;
    background: rgba(0, 0, 0, 0.95);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 8px 0;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    z-index: 1000;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }
  .nav-dropdown:hover .nav-dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .nav-dropdown-item {
    list-style: none;
  }
  .nav-dropdown-link {
    display: block;
    padding: 12px 20px;
    color: var(--gray-light);
    text-decoration: none;
    font-family: "Merriweather", serif;
    font-size: 0.85rem;
    font-weight: 400;
    transition: all 0.3s ease;
  }
  .nav-dropdown-link:hover {
    color: var(--gold);
    background: rgba(194, 143, 36, 0.1);
  }
  /* ============================================
     RESPONSIVE STYLES
  ============================================ */
  @media (max-width: 1024px) {
    .nav-list {
      gap: 4px;
    }

    .nav-link {
      padding: 10px 14px;
      font-size: 0.85rem;
    }
  }

  @media (max-width: 768px) {
    .header {
      height: var(--header-height-mobile);
    }

    .header-container {
      padding: 0 20px;
    }

    .logo-image {
      height: 44px;
    }

    .nav-toggle {
      display: flex;
    }

    .nav-list {
      position: fixed;
      top: var(--header-height-mobile);
      left: 0;
      right: 0;
      bottom: 0;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 16px;
      background: rgba(0, 0, 0, 0.98);
      backdrop-filter: blur(20px);
      opacity: 0;
      visibility: hidden;
      transform: translateY(-20px);
      transition: all 0.4s ease;
    }

    .nav-list.active {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
    }

    .nav-link {
      font-size: 1.1rem;
      padding: 16px 24px;
    }
  }
</style>

<script>
  // Toggle mobile navigation
  const navToggle = document.getElementById("nav-toggle");
  const navList = document.getElementById("nav-list");

  if (navToggle && navList) {
    navToggle.addEventListener("click", () => {
      navToggle.classList.toggle("active");
      navList.classList.toggle("active");
      const isExpanded = navToggle.getAttribute("aria-expanded") === "true";
      navToggle.setAttribute("aria-expanded", (!isExpanded).toString());
    });

    // Close menu when clicking a link
    const navLinks = navList.querySelectorAll(".nav-link");
    navLinks.forEach((link) => {
      link.addEventListener("click", () => {
        navToggle.classList.remove("active");
        navList.classList.remove("active");
        navToggle.setAttribute("aria-expanded", "false");
      });
    });
  }
</script>

<!-- ============================================
     HEADER CON MENÚ DESPLEGABLE (COMENTADO)
     Para usar el menú desplegable, descomenta el bloque de abajo
     y comenta o elimina el header original de arriba.
     ============================================ -->

<!--
<header class="header-dropdown" id="main-header">
  <div class="header-dropdown-backdrop"></div>
  <div class="header-dropdown-container">
    <a href="/" class="logo-link-dropdown group">
      <div class="flex items-center gap-4">
        <img
          src="/logosimple.webp"
          alt="Logo de la Fundación Orquesta Sinfónica de Carabobo"
          class="logo-image-dropdown drop-shadow-lg transition-transform duration-300 group-hover:scale-105"
        />
        <div class="hidden md:flex flex-col">
          <span
            class="text-xs font-light tracking-widest text-gray-300 uppercase leading-tight"
          >
            Fundación
          </span>
          <span
            class="text-sm font-bold tracking-wide text-white leading-tight font-serif"
          >
            Orquesta Sinfónica
          </span>
          <span
            class="text-xs font-medium tracking-wider text-accent-yellow uppercase leading-tight"
          >
            de Carabobo
          </span>
        </div>
      </div>
    </a>

    <button
      class="menu-dropdown-toggle"
      id="menu-dropdown-toggle"
      aria-label="Abrir menú de navegación"
      aria-expanded="false"
    >
      <span class="menu-toggle-text">Menú</span>
      <div class="menu-toggle-icon">
        <span class="menu-line"></span>
        <span class="menu-line"></span>
        <span class="menu-line"></span>
      </div>
    </button>
  </div>

  <div class="dropdown-panel" id="dropdown-panel">
    <nav class="dropdown-nav">
      <ul class="dropdown-nav-list">
        {
          navLinks.map((link, index) => (
            <li class="dropdown-nav-item" style={`--delay: ${index * 0.05}s`}>
              <a
                href={link.href}
                class={`dropdown-nav-link ${link.active ? "active" : ""}`}
              >
                <span class="dropdown-link-number">0{index + 1}</span>
                <span class="dropdown-link-text">{link.label}</span>
                <span class="dropdown-link-arrow">→</span>
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <div class="dropdown-footer">
      <div class="dropdown-contact">
        <a href="mailto:sinfocarabobo@gmail.com" class="dropdown-contact-link">
          sinfocarabobo@gmail.com
        </a>
        <a href="tel:04143410642" class="dropdown-contact-link">
          +58 0414-341-0642
        </a>
      </div>
      <div class="dropdown-social">
        <a href="https://www.facebook.com/sinfonicarabobo/" target="_blank" rel="noopener noreferrer" aria-label="Facebook" class="dropdown-social-link">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
        </a>
        <a href="https://www.instagram.com/sinfocarabobo" target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="dropdown-social-link">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
        </a>
        <a href="https://x.com/SinfoniCarabobo" target="_blank" rel="noopener noreferrer" aria-label="X" class="dropdown-social-link">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
        </a>
        <a href="https://www.youtube.com/@sinfocarabobo" target="_blank" rel="noopener noreferrer" aria-label="YouTube" class="dropdown-social-link">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
        </a>
      </div>
    </div>
  </div>
</header>

<style>
  :root {
    --header-height: 80px;
    --header-height-mobile: 64px;
    --gold: #c28f24;
    --gold-light: #ddb850;
    --black: #000000;
    --white: #ffffff;
    --gray-light: #e0e0e0;
  }

  .header-dropdown {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: var(--header-height);
    z-index: 1000;
  }

  .header-dropdown-backdrop {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.95) 0%,
      rgba(0, 0, 0, 0.85) 50%,
      rgba(0, 0, 0, 0.7) 100%
    );
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }

  .header-dropdown-container {
    position: relative;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 32px;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo-link-dropdown {
    position: relative;
    display: flex;
    align-items: center;
    text-decoration: none;
    z-index: 10;
  }

  .logo-image-dropdown {
    height: 56px;
    width: auto;
    object-fit: contain;
    filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.3));
  }

  .menu-dropdown-toggle {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 20px;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 1001;
  }

  .menu-dropdown-toggle:hover {
    border-color: var(--gold);
    background: rgba(194, 143, 36, 0.1);
  }

  .menu-dropdown-toggle.active {
    border-color: var(--gold);
    background: rgba(194, 143, 36, 0.15);
  }

  .menu-toggle-text {
    font-family: "Merriweather", serif;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--white);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .menu-toggle-icon {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 4px;
    width: 20px;
    height: 14px;
  }

  .menu-line {
    display: block;
    width: 100%;
    height: 2px;
    background: var(--gold);
    border-radius: 2px;
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .menu-dropdown-toggle.active .menu-line:nth-child(1) {
    transform: rotate(45deg) translate(4px, 4px);
  }

  .menu-dropdown-toggle.active .menu-line:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }

  .menu-dropdown-toggle.active .menu-line:nth-child(3) {
    transform: rotate(-45deg) translate(4px, -4px);
  }

  .dropdown-panel {
    position: fixed;
    top: var(--header-height);
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.98);
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-20px);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 60px 40px 40px;
    overflow-y: auto;
  }

  .dropdown-panel.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-nav {
    max-width: 600px;
    margin: 0 auto;
    width: 100%;
  }

  .dropdown-nav-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .dropdown-nav-item {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.4s ease;
    transition-delay: var(--delay);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .dropdown-panel.active .dropdown-nav-item {
    opacity: 1;
    transform: translateY(0);
  }

  .dropdown-nav-link {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 24px 0;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .dropdown-nav-link:hover {
    padding-left: 16px;
  }

  .dropdown-link-number {
    font-family: "Merriweather", serif;
    font-size: 0.75rem;
    color: var(--gold);
    opacity: 0.6;
    min-width: 24px;
  }

  .dropdown-link-text {
    font-family: "Playfair Display", serif;
    font-size: 1.8rem;
    font-weight: 500;
    color: var(--white);
    transition: color 0.3s ease;
    flex: 1;
  }

  .dropdown-nav-link:hover .dropdown-link-text {
    color: var(--gold);
  }

  .dropdown-link-arrow {
    font-size: 1.2rem;
    color: var(--gold);
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
  }

  .dropdown-nav-link:hover .dropdown-link-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  .dropdown-nav-link.active .dropdown-link-text {
    color: var(--gold);
  }

  .dropdown-footer {
    max-width: 600px;
    margin: 0 auto;
    width: 100%;
    padding-top: 40px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 20px;
  }

  .dropdown-contact {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .dropdown-contact-link {
    font-family: "Merriweather", serif;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .dropdown-contact-link:hover {
    color: var(--gold);
  }

  .dropdown-social {
    display: flex;
    gap: 16px;
  }

  .dropdown-social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.6);
    transition: all 0.3s ease;
  }

  .dropdown-social-link:hover {
    border-color: var(--gold);
    color: var(--gold);
    background: rgba(194, 143, 36, 0.1);
  }

  @media (max-width: 768px) {
    .header-dropdown {
      height: var(--header-height-mobile);
    }

    .header-dropdown-container {
      padding: 0 20px;
    }

    .logo-image-dropdown {
      height: 44px;
    }

    .menu-toggle-text {
      display: none;
    }

    .menu-dropdown-toggle {
      padding: 10px 14px;
    }

    .dropdown-panel {
      top: var(--header-height-mobile);
      padding: 40px 24px 30px;
    }

    .dropdown-link-text {
      font-size: 1.4rem;
    }

    .dropdown-nav-link {
      padding: 18px 0;
    }

    .dropdown-footer {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>

<script>
  const menuToggle = document.getElementById("menu-dropdown-toggle");
  const dropdownPanel = document.getElementById("dropdown-panel");

  if (menuToggle && dropdownPanel) {
    menuToggle.addEventListener("click", () => {
      menuToggle.classList.toggle("active");
      dropdownPanel.classList.toggle("active");
      const isExpanded = menuToggle.getAttribute("aria-expanded") === "true";
      menuToggle.setAttribute("aria-expanded", (!isExpanded).toString());

      if (dropdownPanel.classList.contains("active")) {
        document.body.style.overflow = "hidden";
      } else {
        document.body.style.overflow = "";
      }
    });

    const navLinks = dropdownPanel.querySelectorAll(".dropdown-nav-link");
    navLinks.forEach((link) => {
      link.addEventListener("click", () => {
        menuToggle.classList.remove("active");
        dropdownPanel.classList.remove("active");
        menuToggle.setAttribute("aria-expanded", "false");
        document.body.style.overflow = "";
      });
    });

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && dropdownPanel.classList.contains("active")) {
        menuToggle.classList.remove("active");
        dropdownPanel.classList.remove("active");
        menuToggle.setAttribute("aria-expanded", "false");
        document.body.style.overflow = "";
      }
    });
  }
</script>
-->
