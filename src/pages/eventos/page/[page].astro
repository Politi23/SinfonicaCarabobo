---
export const prerender = false;

const parsedPage = Number.parseInt(Astro.params.page ?? "1", 10);

return Astro.redirect(
  `/eventos?page=${Number.isFinite(parsedPage) && parsedPage > 0 ? parsedPage : 1}`,
  301,
);
---
